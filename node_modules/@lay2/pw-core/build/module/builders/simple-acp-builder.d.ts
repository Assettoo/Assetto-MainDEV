import { Builder, BuilderOption } from '../builders/builder';
import { Address, Amount, Cell, Transaction } from '../models';
export declare class SimpleACPBuilder extends Builder {
    protected address: Address;
    protected amount: Amount;
    protected options: BuilderOption;
    receiverInputCell: Cell;
    receiverOutputCell: Cell;
    constructor(address: Address, amount: Amount, options?: BuilderOption);
    build(): Promise<Transaction>;
    buildSenderCells(fee?: Amount): Promise<Transaction>;
    getCollector(): import("..").Collector | import("..").SUDTCollector;
}
