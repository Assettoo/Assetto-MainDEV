import { CollectorOptions } from './collector';
import { SUDTCollector } from './sudt-collector';
import { Cell, Address, Amount, SUDT } from '..';
export declare class PwCollector extends SUDTCollector {
    apiBase: string;
    constructor(apiBase: string);
    getBalance(address: Address): Promise<Amount>;
    collect(address: Address, options: CollectorOptions): Promise<Cell[]>;
    getSUDTBalance(sudt: SUDT, address: Address): Promise<Amount>;
    collectSUDT(sudt: SUDT, address: Address, options: CollectorOptions): Promise<Cell[]>;
}
