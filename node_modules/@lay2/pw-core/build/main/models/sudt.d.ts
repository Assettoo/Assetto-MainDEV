import { Script } from '.';
export interface SudtInfo {
    symbol: string;
    decimals: number;
    name: string;
}
export declare class SUDT {
    readonly issuerLockHash: string;
    readonly info?: SudtInfo;
    constructor(issuerLockHash: string, info?: SudtInfo);
    toTypeScript(): Script;
}
