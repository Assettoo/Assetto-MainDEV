import { CKBModel, WitnessArgs } from '../interfaces';
import { RawTransaction } from '.';
export declare class Transaction implements CKBModel {
    raw: RawTransaction;
    witnessArgs: (WitnessArgs | string)[];
    witnesses: string[];
    constructor(raw: RawTransaction, witnessArgs: (WitnessArgs | string)[], _witnessLengths?: number[]);
    sameWith(tx: Transaction): boolean;
    getSize(): number;
    validate(): Transaction;
    transform(): object;
    serializeJson(): object;
}
