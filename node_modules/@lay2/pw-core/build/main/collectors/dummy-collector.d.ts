import { Cell, Address, Amount } from '..';
import { SUDT } from '../models';
import { SUDTCollector } from './sudt-collector';
export declare class DummyCollector extends SUDTCollector {
    getBalance(): Promise<Amount>;
    constructor();
    collect(address: Address): Promise<Cell[]>;
    getSUDTBalance(): Promise<Amount>;
    collectSUDT(sudt: SUDT, address: Address): Promise<Cell[]>;
}
