import { Collector } from '../collectors/collector';
import { Amount, Transaction } from '../models';
import { WitnessArgs } from '../interfaces';
import { SUDTCollector } from '../collectors/sudt-collector';
export interface BuilderOption {
    feeRate?: number;
    collector?: Collector;
    witnessArgs?: WitnessArgs;
    data?: string;
}
export declare abstract class Builder {
    protected feeRate: number;
    protected collector: Collector | SUDTCollector;
    protected witnessArgs: WitnessArgs;
    static readonly MIN_FEE_RATE = 1000;
    static readonly MIN_CHANGE: Amount;
    static readonly WITNESS_ARGS: {
        Secp256k1: {
            lock: string;
            input_type: string;
            output_type: string;
        };
        RawSecp256k1: {
            lock: string;
            input_type: string;
            output_type: string;
        };
        Secp256r1: {
            lock: string;
            input_type: string;
            output_type: string;
        };
    };
    static calcFee(tx: Transaction, feeRate?: number): Amount;
    protected fee: Amount;
    protected constructor(feeRate?: number, collector?: Collector | SUDTCollector, witnessArgs?: WitnessArgs);
    getFee(): Amount;
    abstract build(): Promise<Transaction>;
}
