import { Builder } from '../builders/builder';
import { Address, Amount, Transaction } from '../models';
import { BuilderOption } from './builder';
export declare class SimpleBuilder extends Builder {
    private address;
    private amount;
    protected options: BuilderOption;
    simpleACPBuilder: Builder;
    constructor(address: Address, amount: Amount, options?: BuilderOption);
    build(fee?: Amount): Promise<Transaction>;
    getCollector(): import("..").Collector | import("..").SUDTCollector;
}
