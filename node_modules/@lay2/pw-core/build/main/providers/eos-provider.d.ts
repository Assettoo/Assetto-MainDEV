import { Provider } from './provider';
import { Address } from '../models';
export declare class EosProvider extends Provider {
    readonly eosNetwork: any;
    onAddressChanged: (newAddress: Address) => void;
    constructor(eosNetwork: any, onAddressChanged?: (newAddress: Address) => void);
    init(): Promise<Provider>;
    getEosPublicKey(account: string): Promise<any>;
    getCKBLockArgsForEosAccount(account: string): Promise<string>;
    processEosHash(a: any): any;
    sign(message: string): Promise<string>;
    close(): void;
}
