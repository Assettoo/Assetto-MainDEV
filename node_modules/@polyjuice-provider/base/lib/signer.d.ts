declare global {
    interface Window {
        ethereum: any;
    }
}
export default class Signer {
    private_key: string | undefined;
    constructor(private_key?: string);
    sign_with_metamask(message_without_prefix: string, address: string): Promise<string>;
    sign_with_private_key(message_without_prefix: string): string;
}
